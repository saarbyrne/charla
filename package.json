{
  "name": "charla",
  "version": "0.1.0",
  "private": true,
  "description": "Daily live Spanish conversation practice with memory",
  "type": "module",
  "scripts": {
    "start": "node scripts/serve.mjs --port 5174",
    "start:lan": "node scripts/serve.mjs --port 5174 --host",
    "test": "node --test \"tests/*.test.js\"",
    "precache": "node scripts/precache.mjs",
    "typecheck": "tsc -p jsconfig.json"
  },
  "engines": { "node": ">=22" }
}
